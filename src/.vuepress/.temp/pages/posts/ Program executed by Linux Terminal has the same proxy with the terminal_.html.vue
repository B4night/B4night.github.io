<template><div><h1 id="program-executed-by-linux-terminal-has-the-same-proxy-with-the-terminal" tabindex="-1"><a class="header-anchor" href="#program-executed-by-linux-terminal-has-the-same-proxy-with-the-terminal"><span>Program executed by Linux Terminal has the same proxy with the terminal?</span></a></h1>
<p>Today I ran into an awkward situation. I am in China right now and I need proxy to access internet. So I set the global proxy of terminal.</p>
<p>I executed a program and it needs to upload image to image bed, which is an server in China. The procedure is: the program executes uploading commands via terminal, while the terminal has proxy settings. So it’s really awkward that the uploading failed.</p>
<p>I spent sevel hours for debugging and write this blog for people whom have ran into the same pitfall.</p>
</div></template>


